name: Deploy
on:
  push:
    branches: main
  pull_request:
    branches: main

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Needed for auth with Deno Deploy
      contents: read # Needed to clone the repository
    env:
      PUBLIC_SECRET_KEY: #12345
    steps:
      - name: Clone repository
        uses: actions/checkout@v3

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8.5.1

      - name: Base Install
        run: pnpm install

      - name: Build step
        run: pnpm run build # 📝 Update the build command(s)

      - name: List
        run: ls -la

      - name: Clean Up
        run: rm -rf node_modules

      - name: Upload to Deno Deploy
        uses: denoland/deployctl@v1
        with:
          project: "rodgco-rodburger"
          entrypoint: ".svelte-kit/output/server.js" # same as `out` option in config
